name: build

on: [push, pull_request]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cups-pdf
      - name: Run tests
        run: |
          lpr -P PDF sample.pdf
          ls -al $HOME/PDF
          